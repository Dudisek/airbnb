<div class="container">
<div id="clearance" class="sign-up">
<%byebug%>
  <p>
    <a href="/auth/facebook"><img class="fb" src="/img/fbconnect.png" /></a>
  </p>

  <h2><%= t(".title") %></h2>

  <%= form_for @user do |form| %>
    <%= render partial: '/users/form', object: form %>

    <div class="other-links">
      <%= link_to t(".sign_in"), sign_in_path %>
    </div>
  <% end %>
</div>
</div>